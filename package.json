{
  "name": "parselfee",
  "version": "1.0.0",
  "description": "Hostel parcel delivery service for RCOEM students",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check config.js && node --check server.js && node --check db/schema.js && node --check middleware/auth.js && node --check middleware/csrf.js && node --check routes/auth.js && node --check routes/orders.js && node --check routes/profile.js && node --check public/app.js",
    "test": "node --test",
    "audit": "npm audit --audit-level=moderate",
    "verify": "npm run check && npm test && npm run audit"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
